import React from 'react';
import reduxify from '../../util/reduxify';

const Dispute = (props) =>
  (
    <div>
      <div>Dispute
      </div>
    </div>
  )

export default reduxify({}, [], Dispute);
